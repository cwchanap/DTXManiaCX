coverage:
  status:
    project:
      default:
        target: 85%
        threshold: 0%
        informational: false
        if_not_found: failure
    patch:
      default:
        target: 85%
        threshold: 0%
        informational: false
        if_not_found: failure
